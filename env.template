# Supabase Configuration
# Get these values from your Supabase project settings
# https://app.supabase.com/project/_/settings/api

VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase Service Key (for admin operations)
# Get this from your Supabase project settings > API > service_role key
SUPABASE_SERVICE_KEY=your-service-key-here

# Email Configuration (for both local development and production)
# IMPORTANT: Use .env.local for development, Vercel dashboard for production
EMAIL_PROVIDER=gmail
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=465
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-specific-password
EMAIL_FROM="Unido <noreply@unido.com>"

# API Base URL (optional - auto-detected by Vercel)
# Leave empty for auto-detection
# VITE_API_BASE_URL=

# Instructions:
# 1. For DEVELOPMENT: Create a .env.local file in the project root
# 2. For PRODUCTION: Set these variables in Vercel dashboard
# 3. Copy the contents of this file to your .env.local
# 4. Replace the placeholder values with your actual credentials
# 5. Never commit .env.local to version control (already in .gitignore)

# Development Usage:
# - Run development: npm run dev (now uses vercel dev - sends REAL emails)
# - Build for production: npm run build
# - Preview production build: npm run preview

# Production URL for email templates (frontend)
VITE_PRODUCTION_URL=https://unido-lu7i.vercel.app

# Production URL for email templates (backend/serverless)
PRODUCTION_URL=https://unido-lu7i.vercel.app

# Gmail App Password Setup:
# 1. Enable 2-Factor Authentication on your Gmail account
# 2. Go to Google Account Settings > Security > App passwords
# 3. Generate an app password for "Mail"
# 4. Use this app password (not your regular password) in EMAIL_PASSWORD

